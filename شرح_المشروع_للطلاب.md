# شرح مشروع Belle Beauty Salon — دليل تعليمي للطلاب

> هذا الملف موجّه لطالب في تخصص تقنية المعلومات (IT) يريد أن يفهم **ماذا تم بناؤه في هذا المشروع، وكيف، ولماذا**. لا يقتصر الشرح على سرد التعديلات، بل يشرح المفاهيم البرمجية التي تقف خلف كل خطوة، لكي يستفيد الطالب من المشروع كمادة تعليمية حقيقية، وليس فقط كسجل تغييرات.

---

## جدول المحتويات

1. [نظرة عامة على المشروع](#1-نظرة-عامة-على-المشروع)
2. [البنية العامة: كيف يتواصل التطبيق مع الخادم؟](#2-البنية-العامة-كيف-يتواصل-التطبيق-مع-الخادم)
3. [قاعدة البيانات: MongoDB](#3-قاعدة-البيانات-mongodb)
4. [الخادم الخلفي (Backend): Node.js + Express](#4-الخادم-الخلفي-backend-nodejs--express)
5. [نظام تسجيل الدخول والمصادقة (Authentication) باستخدام JWT](#5-نظام-تسجيل-الدخول-والمصادقة-authentication-باستخدام-jwt)
6. [الواجهة الأمامية (Frontend): Flutter بالتفصيل](#6-الواجهة-الأمامية-frontend-flutter-بالتفصيل)
   - [6.1 لغة البرمجة: Dart](#61-لغة-البرمجة-dart)
   - [6.2 إطار العمل Flutter](#62-إطار-العمل-flutter-فكرة-كل-شيء-widget)
   - [6.3 هيكلة مجلد lib/](#63-كيف-يُنظَّم-مجلد-lib)
   - [6.4 كل الحزم المستخدمة ولماذا](#64-كل-الحزم-packages-المستخدمة-في-pubspecyaml-ولماذا)
   - [6.5 ملف api_service.dart](#65-الملف-الأهم-في-كل-الفرونت-إند-libservicesapi_servicedart)
   - [6.6 الـ Controllers](#66-كل-الـ-controllers-التي-رُبطت-بالخادم-الحقيقي-بدل-البيانات-المزيّفة)
7. [درس أمني مهم: مفتاح الذكاء الاصطناعي (Groq API Key)](#7-درس-أمني-مهم-مفتاح-الذكاء-الاصطناعي-groq-api-key)
8. [إصلاح أخطاء حقيقية وجدت أثناء العمل (Debugging)](#8-إصلاح-أخطاء-حقيقية-وجدت-أثناء-العمل-debugging)
9. [أمور تُركت عمداً غير مكتملة (Scope Management)](#9-أمور-تركت-عمداً-غير-مكتملة-scope-management)
10. [خطوات تشغيل المشروع محلياً](#10-خطوات-تشغيل-المشروع-محلياً)
11. [خلاصة: أهم المفاهيم التي تعلمناها من هذا المشروع](#11-خلاصة-أهم-المفاهيم-التي-تعلمناها-من-هذا-المشروع)

---

## 1. نظرة عامة على المشروع

**Belle Beauty Salon** هو تطبيق لحجز مواعيد صالون تجميل، يتكوّن من جزأين أساسيين:

- **`frontend/`**: تطبيق موبايل مبني بلغة **Dart** باستخدام إطار عمل **Flutter**. هذا هو الجزء الذي يراه المستخدم على شاشة هاتفه.
- **`backend/`**: خادم (server) مبني بلغة **JavaScript** باستخدام **Node.js** و **Express**، مسؤول عن تخزين البيانات ومعالجة الطلبات (تسجيل الدخول، الحجوزات، الخدمات، إلخ).

### كيف كان المشروع قبل هذا العمل؟

قبل هذا التطبيق العملي، كان `frontend/` تطبيقاً **يعمل بمفرده (standalone)** — كل البيانات التي يراها المستخدم (الفئات، الخدمات، المستخدم نفسه) كانت بيانات **مُقلَّدة ومكتوبة يدوياً (hardcoded / mock data)** داخل كود Flutter نفسه. أما `backend/` فكان موجوداً كمشروع منفصل تقريباً، لم يكن مربوطاً بالتطبيق فعلياً.

> **مفهوم مهم:** الفرق بين "mock data" و"real data" هو فرق جوهري في هندسة البرمجيات. أثناء التطوير المبكر، من الطبيعي أن يكتب المطوّر بيانات وهمية داخل الكود لتجربة الواجهات بسرعة دون انتظار جهوزية الخادم. لكن هذا يعني أن التطبيق لا "يتذكر" أي شيء — إذا أغلقت التطبيق وأعدت فتحه، أو استخدمه مستخدم آخر، فلن يرى نفس البيانات الحقيقية. **الهدف من هذا العمل هو الانتقال من مرحلة "mock data" إلى مرحلة "full-stack حقيقي"** حيث التطبيق يقرأ ويكتب فعلياً من قاعدة بيانات دائمة.

---

## 2. البنية العامة: كيف يتواصل التطبيق مع الخادم؟

هذا المشروع يتبع نموذج **Client-Server** (عميل-خادم) الكلاسيكي في هندسة الويب:

```
┌─────────────────────┐         HTTP Requests          ┌──────────────────────┐
│   Flutter App        │  ────────────────────────────▶ │   Express Server      │
│   (frontend/)         │                                 │   (backend/)           │
│                       │  ◀──────────────────────────── │                        │
│  - شاشات المستخدم     │         JSON Responses          │  - المسارات (Routes)   │
│  - GetX Controllers   │                                 │  - المتحكمات (Controllers) │
│  - api_service.dart   │                                 │  - النماذج (Models)    │
└─────────────────────┘                                 └───────────┬──────────┘
                                                                       │
                                                                       │ Mongoose (ODM)
                                                                       ▼
                                                             ┌──────────────────────┐
                                                             │   MongoDB Database    │
                                                             │  (belle_beauty_salon) │
                                                             └──────────────────────┘
```

- **التطبيق (Client)** لا يتحدث مع قاعدة البيانات مباشرة أبداً — هذا خطأ شائع يجب تجنبه. أي تطبيق يتصل مباشرة بقاعدة بيانات من جهاز المستخدم يكون غير آمن، لأن بيانات الاعتماد (credentials) وقواعد الوصول تصبح مكشوفة على كل جهاز.
- **الخادم (Server / Backend)** هو الوسيط الوحيد المسموح له بالتحدث مع قاعدة البيانات. كل طلب من التطبيق يمرّ عبر HTTP (باستخدام صيغة **REST API**)، والخادم هو من يقرر ماذا يُسمح للمستخدم أن يرى أو يغيّر.
- **البروتوكول المستخدم:** REST عبر JSON — أي أن كل طلب وكل رد يُكتب بصيغة JSON، وكل عملية تُصنَّف بفعل HTTP:
  - `GET` → قراءة بيانات (مثل: جلب قائمة الخدمات)
  - `POST` → إنشاء بيانات جديدة (مثل: تسجيل حساب، إنشاء حجز)
  - `PATCH`/`PUT` → تعديل بيانات موجودة (مثل: تعديل الملف الشخصي)
  - `DELETE` → حذف بيانات (مثل: حذف الحساب)

هذا التصنيف يُسمّى **RESTful API Design**، وهو معيار شائع جداً في صناعة البرمجيات.

---

## 3. قاعدة البيانات: MongoDB

### لماذا MongoDB وليس MySQL أو SQL Server؟

MongoDB هي قاعدة بيانات من نوع **NoSQL** (وتحديداً: **Document Database**). بدلاً من تخزين البيانات في جداول (tables) بصفوف وأعمدة صارمة كما في SQL، تُخزَّن البيانات في **مستندات (Documents)** على شكل JSON. مثال على مستند "خدمة" (Service):

```json
{
  "_id": "665f1a2b3c4d5e6f7a8b9c0d",
  "serviceName": "قص شعر رجالي",
  "price": 50,
  "categoryId": "665f1a2b3c4d5e6f7a8b9c0a",
  "specialistId": "665f1a2b3c4d5e6f7a8b9c0b"
}
```

هذا مناسب جداً لتطبيقات مثل هذا المشروع، حيث تتغير هياكل البيانات بسرعة أثناء التطوير (إضافة حقل جديد لا يتطلب "migration" معقدة كما في SQL).

### أين تم تنصيب قاعدة البيانات؟

- تم تنصيب **MongoDB Community Server** محلياً على المسار `D:\MongoDB` (وليس المسار الافتراضي `C:\Program Files\...`، لأن قرص `C:` كان يحتوي على مساحة فراغ قليلة جداً — 2.6 جيجابايت فقط).
- تعمل كخدمة ويندوز (**Windows Service**) اسمها `MongoDB`، تبدأ تلقائياً مع تشغيل الجهاز، وتستمع على المنفذ (Port) الافتراضي `127.0.0.1:27017`.
- اسم قاعدة البيانات المستخدمة في هذا المشروع: **`belle_beauty_salon`**.
- للتحقق أن الخدمة تعمل (من PowerShell):
  ```powershell
  Get-Service MongoDB
  ```
  يجب أن تكون الحالة `Running`. إن لم تكن كذلك:
  ```powershell
  Start-Service MongoDB
  ```
- يمكن أيضاً فحص البيانات مباشرة عبر أداة سطر الأوامر `mongosh`:
  ```bash
  mongosh mongodb://127.0.0.1:27017/belle_beauty_salon
  ```

### كيف يتحدث Node.js مع MongoDB؟ (Mongoose)

الخادم لا يكتب استعلامات MongoDB الخام مباشرة، بل يستخدم مكتبة تسمى **Mongoose**، وهي ما يسمى **ODM (Object-Document Mapper)** — تسمح بتعريف "نماذج" (Models) بلغة JavaScript، كل نموذج يمثّل نوع مستند، مع تحديد نوع كل حقل، وإن كان مطلوباً (`required`)، وقيمته الافتراضية (`default`)، والعلاقات مع نماذج أخرى (`ref`).

مثال مبسّط عن تعريف نموذج في Mongoose:

```javascript
const serviceSchema = new mongoose.Schema({
  serviceName: { type: String, required: true },
  price: { type: Number, required: true },
  categoryId: { type: mongoose.Schema.Types.ObjectId, ref: "Category" },
  description: { type: String, default: "" }, // ليست إلزامية
});
```

هذا يشبه إلى حد كبير مفهوم **الكلاسات (Classes)** في البرمجة الكائنية، حيث الـ Schema هو "القالب"، والمستند الفعلي في قاعدة البيانات هو "نسخة (instance)" من هذا القالب.

---

## 4. الخادم الخلفي (Backend): Node.js + Express

### نمط التصميم: MVC (Model - View - Controller) المبسّط

الخادم منظّم حسب نمط شائع جداً في تطوير الخوادم (بدون الجزء View لأنه لا واجهة رسومية هنا):

```
backend/src/
  ├── models/       ← تعريف شكل البيانات (Mongoose Schemas)
  ├── controllers/  ← منطق العمل: ماذا يحدث عند كل طلب
  ├── routes/       ← تحديد أي رابط (URL) يذهب إلى أي دالة في الـ controller
  ├── validations/  ← فحص صحة البيانات المُدخلة قبل معالجتها
  ├── middlewares/  ← دوال تُنفَّذ "في الوسط" بين الطلب والرد (مثل: التحقق من تسجيل الدخول)
  └── utils/        ← دوال مساعدة عامة يُعاد استخدامها في أكثر من مكان
```

> **مفهوم مهم — فصل الاهتمامات (Separation of Concerns):** كل ملف له مسؤولية واحدة فقط. الـ Route يحدد "أين"، الـ Controller يحدد "ماذا يحدث"، الـ Model يحدد "شكل البيانات". هذا يجعل الكود أسهل للفهم والتعديل، ويُسهّل على عدة مطورين العمل على نفس المشروع دون تعارض.

### ملف البيئة (`.env`) — لماذا لا نكتب كل شيء في الكود؟

ملف `backend/.env` (غير موجود في Git، فقط `.env.example` كنموذج) يحتوي على:
- `PORT=4000` — رقم المنفذ الذي يستمع عليه الخادم
- `MONGODB_URL` — رابط الاتصال بقاعدة البيانات
- مفاتيح JWT السرية
- بيانات حسابات تجريبية (admin + demo)
- `GROQ_API_KEY` (يُشرح في القسم 7)

> **مفهوم مهم:** أي قيمة "حساسة" أو "قد تختلف بين بيئة التطوير والإنتاج" (كرقم المنفذ، أو كلمات السر، أو المفاتيح السرية) يجب أن توضع في متغيرات البيئة (**Environment Variables**)، لا أن تُكتب مباشرة في الكود (**hardcoded**). هذا يمنع تسريب الأسرار عند رفع الكود إلى GitHub، ويسمح بتغيير الإعدادات دون تعديل الكود نفسه.

**ملاحظة عن رقم المنفذ:** تم تغيير المنفذ من `3000` إلى `4000`، لأن المنفذ `3000` على هذا الجهاز مستخدم فعلاً من مشروع آخر (Next.js) غير مرتبط بهذا التطبيق. هذا يوضّح مفهوم **تعارض المنافذ (Port Conflict)** — لا يمكن لبرنامجين الاستماع على نفس المنفذ في نفس الوقت.

### النماذج (Models) الجديدة التي أُضيفت

| النموذج | الغرض منه |
|---|---|
| `Specialist.js` | يمثّل أحد العاملين في الصالون (الاسم، التخصص، الصورة، التقييم، سنوات الخبرة) |
| `Offer.js` | العروض والتخفيضات التي تظهر في شاشة "Offers" |
| `Notification.js` | إشعارات كل مستخدم (أيقونة الجرس في الشاشة الرئيسية) |
| `BlockedSlot.js` | الساعات التي يُغلقها الأدمن يدوياً (استراحة غداء، يوم عطلة) |

> **ملاحظة تصميمية مهمة:** المواعيد المحجوزة فعلياً (Bookings) **لا** تُخزَّن في `BlockedSlot`، بل تُستخرج مباشرة من مستندات `Appointment` الحقيقية وقت الحاجة. لماذا هذا القرار أفضل؟ لأنه يمنع وجود "نسختين من الحقيقة" (two sources of truth) — لو خزّنا حالة "محجوز" في مكانين مختلفين، قد يحدث تعارض بينهما (مثلاً: حجز أُلغي في مكان واحد ونُسي تحديثه في المكان الآخر). الاعتماد على مصدر واحد للحقيقة (**Single Source of Truth**) هو مبدأ أساسي في تصميم قواعد البيانات.

### أخطاء حقيقية وُجدت وأُصلحت في النماذج القديمة

هذا الجزء **مهم جداً تعليمياً**، لأنه يوضّح كيف تبدو أخطاء البرمجة الحقيقية (Bugs) في مشروع فعلي:

1. **خطأ في `Category.js`:** كان هناك حقل محسوب (**Virtual Field**، أي حقل لا يُخزَّن في قاعدة البيانات بل يُحسب تلقائياً عند القراءة) اسمه `servicesCount`، لكنه كان يبحث عن حقل اسمه `category` في نموذج `Service` — والمشكلة أن هذا الحقل غير موجود أصلاً؛ الاسم الصحيح هو `categoryId`. النتيجة: **كل فئة كانت تظهر بعدد خدمات = 0 دائماً**، حتى لو كانت تحتوي على خدمات فعلية! هذا مثال كلاسيكي على خطأ بسيط (اسم متغير خاطئ) يُسبب مشكلة ظاهرة في واجهة المستخدم.

2. **خطأ في `Appointments.js`:** كان الحقل `staffId` يشير (`ref`) إلى نموذج اسمه `'Staff'`، لكن هذا النموذج **غير موجود في المشروع من الأساس**! معنى ذلك أن أي محاولة لاستخدام `.populate()` (وهي دالة في Mongoose تجلب البيانات الكاملة للعنصر المرتبط، بدل الاقتصار على الـ ID فقط) كانت ستفشل بصمت دون أي رسالة خطأ واضحة. تم تصحيحه بجعله `specialistId` يشير إلى نموذج `Specialist` الجديد.

3. **حقل `phone` في `User.js` كان معلّقاً بالكامل (commented out)** — أي أن رقم الهاتف لم يكن يُحفظ إطلاقاً بغض النظر عمّا يُدخله المستخدم! تم تفعيله (اختياري، وليس فريداً/unique).

> **درس مهم للطالب:** هذه الأمثلة تُبيّن أن القراءة الدقيقة للكود، ومطابقة أسماء الحقول بين النموذج (Model) والاستخدام الفعلي، جزء أساسي من عملية debugging. أخطاء كهذه لا تُسبب "تعطّل" (crash) واضح، بل سلوكاً خاطئاً هادئاً (silent bug) وهو أصعب أنواع الأخطاء اكتشافاً.

### نقاط النهاية (API Endpoints) الجديدة

كل نقطة نهاية موصوفة في `frontend/BACKEND_API_REQUIREMENTS.md` أصبحت مُنفَّذة فعلياً في الخادم:

| ملف المسارات | يُركَّب على | يغطّي |
|---|---|---|
| `auth.routes.js` | `/api/v1/auth` | `POST /register`, `POST /login` (تسجيل دخول مبسّط، يُشرح في القسم 5) |
| `user.routes.js` | `/api/v1/users` | بيانات المستخدم الشخصية، المفضلة، الإشعارات، تغيير كلمة السر |
| `category.routes.js` | `/api/v1/categories` | قائمة الفئات، خدمات كل فئة |
| `service.routes.js` | `/api/v1/services` | الخدمات الأكثر طلباً، البحث، التقييمات، الأوقات المتاحة |
| `booking.routes.js` | `/api/v1/bookings` | إنشاء حجز، إلغاء حجز |
| `offer.routes.js` | `/api/v1/offers` | العروض |
| `chat.routes.js` | `/api/v1/chat` | إرسال رسالة للمساعد الذكي |
| `support.routes.js` | `/api/v1/support` | الأسئلة الشائعة، ساعات العمل |
| `admin.routes.js` | `/api/v1/admin` | كل صلاحيات الأدمن (إحصائيات، إدارة الفئات/الخدمات/الحجوزات) |

> **مفهوم مهم — حماية المسارات (Route Protection):** كل مسارات `admin.routes.js` محمية بواسطة **middleware** واحد يُطبَّق مرة واحدة في أعلى الملف: `auth + role(['admin'])`. هذا يعني أن أي طلب يصل لهذه المسارات يجب أولاً أن يمرّ بفحصين: (1) هل المستخدم مسجّل دخول أصلاً (auth)؟ و(2) هل صلاحيته (role) هي "admin"؟ إن فشل أي فحص، يُرفض الطلب فوراً قبل أن يصل لأي منطق عمل. هذا مثال حقيقي على استخدام **Middleware Chaining** لتطبيق قاعدة أمنية واحدة على مجموعة كاملة من المسارات دون تكرار الكود في كل دالة.

### أدوات مساعدة مشتركة (`utils/`)

- **`formatUser.js`**: الدالة الوحيدة في كل المشروع التي تحوّل مستند `User` (كما يخرج من قاعدة البيانات) إلى الشكل الذي يتوقعه التطبيق (`{id, name, email, phone, role, birthDate, loyaltyPoints}`) — مع ضمان عدم تضمين كلمة السر أبداً في الرد.
- **`formatService.js`**: نفس المبدأ لكن للخدمات.

> **مفهوم مهم — مبدأ DRY (Don't Repeat Yourself):** بدل أن يعيد كل controller (تسجيل، دخول، تعديل ملف، إلخ) كتابة نفس منطق "تحويل بيانات المستخدم إلى الشكل المطلوب"، توجد دالة واحدة يستخدمها الجميع. لو احتجنا لاحقاً إضافة حقل جديد لكل رد يحتوي على مستخدم، نُعدّل مكاناً واحداً فقط بدل البحث في كل الملفات.

### سكربت التعبئة الأولية (`seed.js`)

يُشغَّل بالأمر:
```bash
npm run seed
```

هذا السكربت **يمسح** كل البيانات الموجودة، ثم يُدرج بيانات تجريبية جاهزة: 8 فئات، 41 خدمة (نفس البيانات التي كانت مكتوبة يدوياً في التطبيق سابقاً، نُقلت إلى هنا)، 15 أخصائي، حسابين تجريبيين (زبون وأدمن)، 3 عروض، 4 إشعارات، حجزين، وتقييمين.

> **مفهوم مهم — Seed Scripts:** في أي مشروع حقيقي، تحتاج طريقة سريعة لإعادة قاعدة البيانات إلى "حالة نظيفة معروفة" وقت التطوير أو الاختبار، بدون إدخال كل البيانات يدوياً كل مرة. هذا هو الغرض من سكربت التعبئة (Seed Script) — يمكن تشغيله في أي وقت لإعادة كل شيء لحالة تجريبية ثابتة.

---

## 5. نظام تسجيل الدخول والمصادقة (Authentication) باستخدام JWT

### كيف كان الأمر قبل هذا التغيير؟ (ولماذا كان مشكلة أمنية)

قبل هذا العمل، لم يكن التطبيق يقوم بأي اتصال حقيقي بالشبكة لتسجيل الدخول — كان فقط "يصنع" مستخدماً مزيّفاً في الذاكرة. والأخطر من ذلك: **صلاحية المستخدم (زبون أو أدمن) كانت تُحدَّد من الزر الذي ضغطه المستخدم في شاشة اختيار الحساب، لا من الخادم!** بمعنى آخر، كان يكفي أي شخص أن يضغط زر "Admin" في تلك الشاشة ليُوجَّه مباشرة إلى لوحة تحكم الأدمن، **دون أي حساب أدمن حقيقي أو كلمة سر**.

> **درس أمني أساسي:** لا يجوز **أبداً** أن يقرر التطبيق (Client) بمفرده صلاحيات المستخدم. القرار الأمني الوحيد الصحيح هو أن يُحدَّد كل شيء متعلق بالصلاحيات من طرف **الخادم**، الذي يتحقق من هوية المستخدم الحقيقية أولاً، ثم "يقول" للتطبيق ما هي صلاحيته. لا يمكن الوثوق بأي معلومة يُرسلها التطبيق عن هوية أو صلاحية المستخدم؛ الخادم هو مصدر الحقيقة الوحيد.

### كيف يعمل الحل الجديد؟ (JWT)

- `POST /api/v1/auth/register` → يستقبل `{name, email, phone, password}`، ينشئ الحساب، ويُعيد `{token, user}`.
- `POST /api/v1/auth/login` → يستقبل `{email, password}`، ويُعيد `{token, user}`.

الآن `login()` في `auth_controller.dart` (على جهة Flutter) يقرأ **`user.role` من رد الخادم نفسه** ليحدد إلى أين يوجّه المستخدم — لا من أي زر ضُغط في الشاشة السابقة.

> **ما هو JWT (JSON Web Token)؟** هو "بطاقة تعريف" مُشفَّرة يمنحها الخادم للمستخدم بعد تسجيل دخول ناجح. تحتوي هذه البطاقة على معلومات عن هوية المستخدم، ومُوقَّعة رقمياً بمفتاح سري يعرفه الخادم فقط — مما يجعل أي محاولة لتزوير البطاقة أو تعديلها قابلة للاكتشاف فوراً. بعد الحصول عليها، يُرسل التطبيق هذه البطاقة مع **كل طلب لاحق** في رأس (Header) اسمه:
> ```
> Authorization: Bearer <token>
> ```
> وبهذا لا يحتاج المستخدم لإدخال كلمة السر في كل طلب — يكفي إرسال البطاقة، والخادم يتحقق من توقيعها ليعرف من هو المستخدم.

### أين تُخزَّن هذه البطاقة على الجهاز؟

تُخزَّن عبر مكتبة **`shared_preferences`** (تمت إضافتها كتبعية جديدة في `pubspec.yaml`)، وهي طريقة قياسية لتخزين بيانات صغيرة بشكل دائم على جهاز المستخدم (تشبه إلى حد ما `localStorage` في متصفحات الويب).

> **نقطة مهمة تُركت عمداً كتحدٍّ مستقبلي:** الجلسة (Session) **لا تُستعاد** تلقائياً بعد إغلاق التطبيق وإعادة فتحه — يعود المستخدم دائماً لشاشة الترحيب ويحتاج لتسجيل الدخول من جديد. لإضافة ميزة "تذكرني"، يحتاج التطبيق شاشة بداية (Splash Screen) تتحقق من وجود بطاقة محفوظة قبل تحديد أي شاشة تُفتح أولاً. هذا مثال جيد لتمرين إضافي يمكن للطالب تنفيذه بنفسه.

---

## 6. الواجهة الأمامية (Frontend): Flutter بالتفصيل

### 6.1 لغة البرمجة: Dart

التطبيق مكتوب بالكامل بلغة **Dart**، وهي لغة طوّرتها Google خصيصاً لتكون لغة الأساس لإطار العمل Flutter. لماذا Dart بالتحديد وليست لغة أخرى؟

- **لغة واحدة للمنطق والواجهة معاً**: عكس الأنظمة القديمة التي تحتاج لغة مختلفة لكل نظام تشغيل (Swift لـ iOS، Kotlin/Java لـ Android)، يُكتب كل التطبيق بـ Dart، ثم يُترجم Flutter الواجهة إلى كود أصلي على كل منصة (Android, iOS, Windows, Web...).
- **الترجمة الآنية (Hot Reload)**: أثناء التطوير، تُترجَم Dart بواسطة **JIT** (Just-In-Time)، مما يسمح برؤية أثر أي تعديل في الكود على الشاشة في أقل من ثانية دون إعادة تشغيل التطبيق كاملاً. في نسخة الإصدار النهائي (Release)، تُترجَم بواسطة **AOT** (Ahead-Of-Time) إلى كود أصلي سريع الأداء.
- **Null Safety (الأمان من القيم الفارغة)**: منذ الإصدارات الحديثة من Dart، يجب على المبرمج أن يُصرّح صراحةً إن كان متغيّر يمكن أن يكون `null` أم لا. مثال من الكود الفعلي في المشروع (`api_service.dart`):
  ```dart
  static Future<String?> getToken() async { ... }   // الـ ? تعني: قد يُرجع null (لا يوجد توكن محفوظ)
  if (token != null) headers['Authorization'] = 'Bearer $token';
  ```
  هذا يمنع فئة كاملة من الأخطاء الشائعة جداً في لغات أخرى (الوصول إلى قيمة `null` بالخطأ يُسبب تعطّل التطبيق).

- **البرمجة غير المتزامنة (`async`/`await`/`Future`)**: أي عملية تستغرق وقتاً (طلب شبكة، قراءة ملف) في Dart تُعامَل كـ **`Future`** (وعد بقيمة ستصل لاحقاً)، ويُستخدم `async`/`await` لكتابتها بشكل يبدو متتابعاً وسهل القراءة، مع أنها في الحقيقة لا "تُجمّد" التطبيق أثناء الانتظار. مثال من `api_service.dart`:
  ```dart
  static Future<dynamic> get(String path, {bool auth = false}) async {
    final response = await http
        .get(Uri.parse('$apiBaseUrl$path'), headers: await _headers(auth: auth))
        .timeout(const Duration(seconds: 15));
    return _handle(response);
  }
  ```
  بدون `await`، سيستمر تنفيذ باقي الكود فوراً دون انتظار رد الخادم، وستكون النتيجة غير معروفة بعد. `await` "يوقف" تنفيذ هذه الدالة فقط (وليس التطبيق كله) حتى يصل الرد.

- **معاملات مُسمّاة واختيارية (Named/Optional Parameters)**: `{bool auth = false}` في المثال أعلاه تعني إمكانية استدعاء الدالة مع أو بدون هذا المعامل، وإن لم يُذكر، يُستخدم `false` تلقائياً. هذا يجعل الدوال ذات المعاملات الكثيرة أسهل للقراءة عند الاستدعاء (`ApiService.get('/services', auth: true)` أوضح من تمرير `true` بدون اسم).
- **الاستيفاء النصي (String Interpolation)**: `'$apiBaseUrl$path'` و `'Bearer $token'` — طريقة Dart لإدراج قيمة متغيّر داخل نص مباشرة، بدل الجمع اليدوي بعلامة `+`.

### 6.2 إطار العمل Flutter: فكرة "كل شيء Widget"

في Flutter، **كل عنصر تراه على الشاشة هو Widget** — من أكبر شيء (الشاشة كاملة) إلى أصغر شيء (مسافة فارغة `SizedBox`، أو نص `Text`). الشاشات تُبنى بتركيب Widgets داخل بعضها، كأشجار (**Widget Tree**). هذا المشروع يستخدم **Material Design** (`uses-material-design: true` في `pubspec.yaml`) وهو لغة التصميم القياسية لتطبيقات أندرويد/الويب من Google (أزرار، بطاقات، أيقونات بمظهر موحّد).

### 6.3 كيف يُنظَّم مجلد `lib/`؟

```
lib/
  ├── bindings/       ← ربط الـ Controllers بالذاكرة عند بدء التطبيق (initial_bindings.dart)
  ├── constant/       ← ثوابت عامة: الألوان، الصور، مسارات الشاشات (app_routes.dart)
  ├── models/         ← تعريف شكل البيانات في Dart (ServiceModel, UserModel...)
  ├── services/       ← الاتصال بالخادم (api_service.dart)
  ├── translations/   ← نصوص الترجمة (عربي/إنجليزي)
  ├── utils/          ← دوال مساعدة عامة (relative_time.dart)
  └── views/          ← كل شاشة، ولكل شاشة مجلد فيه: *_screen (الواجهة) + *_controller (المنطق والبيانات)
        ├── home/
        │     ├── home_controller/
        │     ├── home_screen/
        │     └── widget/
        ├── booking/
        ├── auth/
        ├── admin/
        ├── favorite/
        ├── profile/
        ├── chat/
        ├── offers/
        └── rolle/    ← شاشة اختيار نوع الحساب (Role) في بداية التطبيق
```

> **مفهوم مهم:** لكل ميزة (Feature) مجلدها الخاص، وداخله ملف الشاشة (View) منفصل عن ملف الـ Controller. هذا هو تطبيق فعلي لمبدأ **فصل الاهتمامات** الذي شرحناه في الخادم أيضاً: ملف الشاشة يهتم فقط بـ "كيف يبدو الشكل"، وملف الـ Controller يهتم فقط بـ "من أين تأتي البيانات وماذا يحدث عند كل ضغطة زر".

### 6.4 كل الحزم (Packages) المستخدمة في `pubspec.yaml` ولماذا

في Flutter/Dart، الحزم الجاهزة (تُسمّى **Packages**) تُنشر على موقع [pub.dev](https://pub.dev) ويُعلَن الاعتماد عليها في ملف `pubspec.yaml`. هذا هو نفس مفهوم `package.json` في Node.js أو `requirements.txt` في Python — ملف واحد يسرد كل الحزم الخارجية التي يعتمد عليها المشروع، مع رقم إصدار كل حزمة، ليستطيع أي شخص آخر تنصيب نفس النسخ بالضبط عبر أمر واحد (`flutter pub get`).

| الحزمة | الإصدار | الغرض منها في هذا المشروع بالتحديد |
|---|---|---|
| `get` | `^4.7.3` | **GetX** — إدارة الحالة، التنقل بين الشاشات، وحقن الاعتماديات (كل شيء في حزمة واحدة) |
| `flutter_screenutil` | `^5.9.3` | جعل التصميم متجاوباً (Responsive) على أحجام شاشات مختلفة |
| `flutter_svg` | `^2.3.0` | عرض صور بصيغة SVG (رسومات متجهية) |
| `google_fonts` | `^6.2.1` | استخدام خطوط من مكتبة Google Fonts دون تضمين ملفاتها في المشروع |
| `http` | `^1.2.0` | تنفيذ طلبات HTTP الحقيقية إلى الخادم (GET/POST/PATCH/PUT/DELETE) |
| `shared_preferences` | `^2.5.5` | تخزين دائم صغير على جهاز المستخدم — يُستخدم هنا فقط لحفظ بطاقة تسجيل الدخول (JWT) |
| `cupertino_icons` | `^1.0.8` | أيقونات بمظهر iOS (Cupertino)، تأتي افتراضياً مع أي مشروع Flutter جديد |
| `flutter_lints` *(dev)* | `^6.0.0` | قواعد تحليل الكود الثابت (Linting) — تنبيهات أسلوبية أثناء الكتابة، لا تدخل في نسخة التطبيق النهائية |
| `flutter_test` *(dev)* | *(من SDK)* | إطار كتابة الاختبارات الآلية في Flutter — موجود جاهزاً من قالب المشروع الافتراضي، لكن **لا توجد اختبارات مكتوبة فعلياً في هذا المشروع بعد** (مجلد `test/` غير موجود) — نقطة تحسين مستقبلية جيدة لأي طالب يريد التمرّن على كتابة Unit Tests |

> **ملاحظة عن `dependencies` مقابل `dev_dependencies`:** الحزم تحت `dependencies` تُشحَن مع التطبيق النهائي (المستخدم يحتاجها لتشغيل التطبيق). الحزم تحت `dev_dependencies` تُستخدم فقط أثناء التطوير (كتابة الاختبارات، فحص جودة الكود) ولا تدخل في حجم أو عمل التطبيق النهائي على جهاز المستخدم.

الآن لنشرح كل حزمة أساسية بتفصيل أكبر، مع أمثلة حقيقية من كود المشروع:

#### أ) `get` (GetX) — العمود الفقري لبنية التطبيق

GetX هي مكتبة "كل شيء داخلها" (all-in-one)، اختيرت هنا بدل بدائل أخرى مثل `Provider` أو `Bloc` لأنها تُقلّل كثيراً من الكود المتكرر (**Boilerplate**) اللازم لإدارة الحالة، وتجمع 4 مهام في نفس الوقت:

**1. إدارة الحالة التفاعلية (Reactive State):** أي متغيّر يُعلَّم بـ `.obs` يصبح "قابلاً للمراقبة" — أي widget يستخدمه داخل `Obx(...)` يُعاد رسمه تلقائياً كل مرة تتغيّر قيمته. مثال حقيقي من `home_controller.dart`:
```dart
class HomeController extends GetxController {
  final selectedCategoryIndex = 0.obs;
  final filterSortBy = 'popular'.obs;
  final isLoading = false.obs;

  bool get isFilterActive =>
      filterSortBy.value != 'popular' || filterMinRating.value > 0;
}
```
هنا `filterSortBy` متغيّر تفاعلي؛ إن غيّرت قيمته من أي مكان في التطبيق (`filterSortBy.value = 'price_asc'`)، فكل واجهة تعرضه بداخل `Obx(() => ...)` تتحدث فوراً دون أي كود يدوي لإعادة الرسم.

**2. التنقل بين الشاشات (Routing):** بدل `Navigator.push` التقليدي في Flutter، يُعرَّف كل مسار مرة واحدة في `app_pages.dart`/`app_routes.dart` (باستخدام أسماء ثابتة مثل `AppRoutes.home = "/homeScreen"`)، ويُستخدم `Get.toNamed(...)` أو `Get.offAllNamed(...)` للتنقل. في `main.dart`:
```dart
return GetMaterialApp(
  initialBinding: InitialBindings(),
  initialRoute: AppRoutes.rolleSceeen,
  getPages: routes,
  translations: AppTranslations(),
  locale: const Locale('ar', 'SA'),
  fallbackLocale: const Locale('ar', 'SA'),
);
```

**3. حقن الاعتماديات (Dependency Injection):** بدل أن تصنع كل شاشة نسخة جديدة من الـ Controller الخاص بها بنفسها، يوجد مكان مركزي (`lib/bindings/initial_bindings.dart`) يُسجّل كل Controller مرة واحدة عند بدء التطبيق:
```dart
class InitialBindings extends Bindings {
  @override
  void dependencies() {
    Get.put(RoleController(), permanent: true);   // يُنشأ فوراً، ويبقى موجوداً طوال حياة التطبيق
    Get.put(AuthController(), permanent: true);
    Get.lazyPut(() => FavoriteController(), fenix: true); // يُنشأ فقط عند أول استخدام له
    Get.lazyPut(() => AdminController(), fenix: true);
  }
}
```
الفرق بين `Get.put` و`Get.lazyPut`: الأول يُنشئ الكائن فوراً عند بدء التطبيق (مناسب لـ `AuthController` الذي يحتاجه التطبيق منذ اللحظة الأولى لمعرفة هل المستخدم مسجّل دخوله)، والثاني "كسول" — لا يُنشئ الكائن إلا عند أول طلب فعلي له (`Get.find<FavoriteController>()`)، مما يوفّر الذاكرة إن لم تُستخدم تلك الشاشة أبداً في جلسة معيّنة. أي مكان آخر في التطبيق يستطيع الوصول لنفس النسخة (وليس نسخة جديدة) عبر:
```dart
final AuthController authController = Get.find<AuthController>();
```

**4. الترجمة (Internationalization / i18n):** بدل مكتبات ترجمة خارجية، تحتوي GetX نظام ترجمة مدمجاً. `lib/translations/app_translations.dart`:
```dart
class AppTranslations extends Translations {
  @override
  Map<String, Map<String, String>> get keys => {
    'en_US': { 'welcome_to': 'WELCOME TO', ... },
    'ar_SA': { 'welcome_to': 'مرحبًا بك في', ... },
  };
}
```
وفي أي مكان بالواجهة، تُستخدم بلاحقة `.tr` (اختصار translate) بدل كتابة النص مباشرة:
```dart
Text('welcome_to'.tr)   // يعرض النص العربي أو الإنجليزي تلقائياً حسب الـ locale الحالي
```
بما أن `locale` و`fallbackLocale` في `main.dart` مضبوطتان على `ar_SA`، فالتطبيق **عربي بشكل افتراضي**.

#### ب) `flutter_screenutil` — تصميم متجاوب على كل الشاشات

مشكلة شائعة جداً: تصميم يبدو مثالياً على شاشة هاتف بمقاس معيّن (مثلاً iPhone) قد يبدو صغيراً جداً أو كبيراً جداً على شاشة أندرويد بمقاس مختلف. حل `flutter_screenutil` هو تحديد "مقاس تصميم مرجعي" (Design Size) مرة واحدة، ثم كل قيمة (عرض، ارتفاع، حجم خط) تُكتب بوحدات نسبية تُحوَّل تلقائياً لحجم الشاشة الفعلي للمستخدم. في `main.dart`:
```dart
ScreenUtilInit(
  designSize: const Size(375, 812),   // التصميم صُمِّم أصلاً بافتراض شاشة 375×812 بكسل
  minTextAdapt: true,
  splitScreenMode: true,
  builder: (context, child) => GetMaterialApp(...),
)
```
وفي كل شاشة، تُستخدم الامتدادات `.w` (عرض)، `.h` (ارتفاع)، `.sp` (حجم خط) بدل أرقام بكسل ثابتة. مثال حقيقي من `home_screen.dart`:
```dart
SizedBox(height: 20.h),
Icon(Icons.spa_outlined, size: 36.sp, color: AppColors.textFaint),
padding: EdgeInsets.symmetric(horizontal: 20.w),
```
لو استُخدم `20` مباشرة بدل `20.h`، سيكون هذا الرقم **ثابتاً بالبكسل الفعلي** بغض النظر عن حجم شاشة المستخدم — فيبدو كبيراً جداً على شاشة صغيرة، وصغيراً جداً على شاشة كبيرة. `20.h` يعني "بمقياس نسبي يعادل 20 بكسل على التصميم المرجعي 375×812، ويُحسب تلقائياً ما يعادله على شاشتك الفعلية".

#### ج) `flutter_svg` — عرض الصور المتجهية (SVG)

صيغة **SVG** (Scalable Vector Graphics) ترسم الصورة بمعادلات رياضية (خطوط، منحنيات) بدل شبكة بكسلات ثابتة كما في PNG/JPG. الميزة: يمكن تكبيرها لأي حجم دون أن تفقد وضوحها أو "تتبكسل"، وحجم ملفها أصغر عادةً للرسومات البسيطة (أيقونات). في هذا المشروع، تُستخدم بشكل رئيسي في شريط التنقل السفلي (`main_screen.dart`) لعرض أيقونات التبويبات.

#### د) `google_fonts` — الخطوط

يوجد نوعان من الخطوط في هذا المشروع، ومن المفيد تعليمياً ملاحظة الفرق بينهما:

1. **خطوط محلية مُضمَّنة (Local Assets)**: مُعرَّفة مباشرة في `pubspec.yaml`:
   ```yaml
   fonts:
     - family: Bukra
       fonts:
         - asset: assets/fonts/29LT_Bukra.ttf
     - family: TimesNewRoman
       fonts:
         - asset: assets/fonts/times_regular.ttf
   ```
   هنا ملف الخط الفعلي (`.ttf`) موجود فعلياً داخل مجلد المشروع ويُشحَن مع التطبيق.

2. **حزمة `google_fonts`**: تسمح باستخدام أي خط من مكتبة Google Fonts الضخمة (أكثر من 1000 خط) **بذكر اسمه فقط** في الكود (`GoogleFonts.poppins(...)` مثلاً)، دون تحميل ملفه يدوياً أو تضمينه في `pubspec.yaml` — تتولى المكتبة تحميله (أو استخدام نسخة مخزّنة مسبقاً/cached) عند الحاجة. تُستخدم في أكثر من 37 ملفاً في هذا المشروع لضبط تنسيق النصوص (الحجم، السماكة، الخط) في كل الشاشات تقريباً.

#### هـ) `http` — تنفيذ طلبات الشبكة الفعلية

هذه هي الحزمة التي **تنفّذ فعلياً** طلبات HTTP التي شرحناها في القسم 2. كل دالة في `api_service.dart` (`get`, `post`, `patch`, `put`, `delete`) تستخدم الدالة المطابقة لها من هذه الحزمة (`http.get(...)`, `http.post(...)`, إلخ)، وتُضيف مهلة زمنية (`timeout`) بـ 15 ثانية لتفادي انتظار التطبيق إلى الأبد إن انقطع الاتصال بالخادم.

#### و) `shared_preferences` — تخزين بطاقة تسجيل الدخول (JWT)

هذه الحزمة توفّر تخزيناً دائماً بسيطاً على شكل مفتاح/قيمة (**Key-Value Storage**) يبقى محفوظاً حتى بعد إغلاق التطبيق. في هذا المشروع، تُستخدم **فقط لهذا الغرض الواحد**: حفظ/قراءة/حذف توكن JWT (المشروح في القسم 5)، من `api_service.dart`:
```dart
static const _tokenKey = 'auth_token';

static Future<void> saveToken(String token) async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.setString(_tokenKey, token);
}

static Future<String?> getToken() async {
  final prefs = await SharedPreferences.getInstance();
  return prefs.getString(_tokenKey);
}

static Future<void> clearToken() async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.remove(_tokenKey);   // يُستخدم عند تسجيل الخروج
}
```

### 6.5 الملف الأهم في كل الفرونت إند: `lib/services/api_service.dart`

هذا هو **المكان الوحيد في كل التطبيق المسموح له بالتحدث مع الشبكة (HTTP)**. كل الـ Controllers تستدعي دوالاً من هذا الملف بدل كتابة كود HTTP في كل مكان بشكل منفصل. بنيته الفعلية (مبسّطة):

- ثابت `apiBaseUrl` يحدد عنوان الخادم (`http://localhost:4000/api/v1`) — مع تعليق في الكود يوضّح أن هذا العنوان يجب أن يتغيّر إن شغّلت التطبيق على محاكي أندرويد (`10.0.2.2`) أو جهاز حقيقي على نفس شبكة الواي فاي (عنوان IP الجهاز)، لأن `localhost` يشير فقط لنفس الجهاز الذي يعمل عليه الخادم.
- كلاس `ApiException` — يحمل رمز الخطأ (`statusCode`) ورسالته (`message`)، بدل أن ترمي دوال الشبكة أخطاء عامة غامضة.
- دالة خاصة `_handle(response)` — نقطة واحدة تفحص كل رد قادم من الخادم: إن كان رمز الحالة (Status Code) 400 أو أكثر، تستخرج رسالة الخطأ الحقيقية من رد الخادم وترميها كـ `ApiException`؛ وإلا تُرجع البيانات المُفكَّكة (decoded) من JSON.
- كل الدوال (`get`, `post`, `patch`, `put`, `delete`) هي **static** (دوال على مستوى الكلاس، لا تحتاج إنشاء نسخة من `ApiService` لاستخدامها) — تُستدعى مباشرة بـ `ApiService.get('/categories')` من أي مكان في التطبيق.

> **مفهوم مهم — طبقة الخدمة (Service Layer):** تجميع كل منطق الاتصال بالشبكة في ملف واحد هو تطبيق عملي لمبدأ فصل الاهتمامات مرة أخرى. لو احتجنا لاحقاً تغيير الرابط الأساسي، أو إضافة إعادة محاولة (retry) عند فشل الاتصال، أو تسجيل (logging) لكل طلب، نُعدّل ملفاً واحداً فقط ويستفيد كل التطبيق من هذا التعديل تلقائياً.

### 6.6 كل الـ Controllers التي رُبطت بالخادم الحقيقي (بدل البيانات المزيّفة)

| Controller | ما أصبح يفعله الآن |
|---|---|
| `AuthController` | تسجيل/دخول/خروج حقيقي، حفظ البطاقة (token)، التوجيه حسب صلاحية قادمة من الخادم |
| `HomeController` | الفئات، الخدمات الشائعة، الإشعارات — كل ذلك من الـ API |
| `CategoryDetailsController` | خدمات كل فئة، مع الترتيب (sort) يُنفَّذ في الخادم |
| `ServiceDetailsController` | قراءة وإرسال التقييمات من/إلى الـ API |
| `BookingController` | الأوقات المتاحة الحقيقية، إنشاء/إلغاء حجز، قائمة "مواعيدي" |
| `FavoriteController` | مزامنة الخدمات/الفئات المفضلة مع الخادم |
| `OffersController` | العروض والوسوم الرائجة من الـ API |
| `ChatController` | يستدعي وسيط الدردشة في الخادم (بدون أي مفتاح مكشوف في التطبيق، انظر القسم 7) |
| `ProfileController` | حفظ التعديلات عبر `PATCH /users/me` |
| `SettingsController` | تغيير كلمة السر وحذف الحساب فعلياً عبر الخادم |
| `AdminController` | كل بيانات لوحة التحكم من الـ API |

### 6.7 تحديثات على النماذج (Models) في Flutter

- **`ServiceModel`**: أُضيف حقل `id` كان مفقوداً بالكامل — بدونه، لا يمكن لأي شاشة تفاصيل خدمة أن "تعرف" أي خدمة بالضبط يجب حجزها أو تقييمها أو إضافتها للمفضلة.
- **`UserModel`**: أُضيف `id`، `role`، ودالة `fromJson` لتحويل رد الخادم (JSON) إلى كائن Dart مباشرة.

> **مفهوم مهم — التسلسل (Serialization/Deserialization):** كل بيانات تنتقل عبر الشبكة تكون بصيغة نص (JSON)، لكن الكود في Flutter يحتاج التعامل معها كأنها كائنات Dart حقيقية (Objects). دالة `fromJson` هي الجسر بين الصيغتين — تأخذ `Map<String, dynamic>` (JSON مُفكَّك) وتُرجع كائن `UserModel` منظّم بحقول واضحة.

---

## 7. درس أمني مهم: مفتاح الذكاء الاصطناعي (Groq API Key)

هذا القسم من أهم الدروس الأمنية في هذا المشروع بالكامل.

### المشكلة الأصلية

كان مفتاح **Groq API** (يُستخدم لتشغيل ميزة الدردشة الذكية Chat) مكتوباً **مباشرة داخل كود التطبيق (Client-side)**. أي شخص يستطيع فتح ملفات التطبيق (أو حتى فحص حركة الشبكة) كان يستطيع رؤية هذا المفتاح واستخدامه بحسابه الخاص — وهذا يعني تكاليف مالية أو استخدام مسيء يتحمّله صاحب الحساب الأصلي.

> **درس أساسي: أي مفتاح API "سري" لا يجوز أبداً أن يوجد داخل تطبيق يُثبَّته المستخدم على جهازه.** أي كود يعمل على جهاز المستخدم (تطبيق موبايل، أو حتى كود JavaScript في متصفح) **يمكن استخراج أي نص مكتوب فيه دائماً**، بغض النظر عن أي "تشفير" بسيط قد يُضاف. القاعدة الذهبية: الأسرار تبقى فقط على الخادم.

### الحل المطبَّق

1. **مفتاح Groq يُخزَّن في قاعدة البيانات** (نموذج جديد `Setting.js`، وهو مجرد مخزن بسيط `{key, value}`)، وليس فقط في ملف `.env`.
2. `POST /api/v1/chat/message` في الخادم يقرأ المفتاح من قاعدة البيانات أولاً، ويرجع لملف `.env` فقط إن لم يكن محفوظاً في القاعدة.
3. **التطبيق لا يعرف المفتاح أبداً** — فقط الأدمن، من داخل لوحة التحكم (زر بشكل أيقونة روبوت 🤖 قرب زر تسجيل الخروج)، يستطيع كتابة/تحديث المفتاح، والذي يُرسَل مباشرة إلى الخادم وليس للتطبيق العادي (endpoint خاص يُعيد فقط `{configured: true/false}` — أي "هل المفتاح موجود أم لا"، دون كشف قيمته الفعلية أبداً حتى للأدمن نفسه).

### درس إضافي: تسريب المفتاح في تاريخ Git

**المفتاح القديم لا يزال موجوداً في تاريخ Git (git history) لهذا المشروع**، حتى بعد حذفه من الملفات الحالية. لماذا؟ لأن Git يحفظ **كل نسخة سابقة** من كل ملف — حذف نص من الملف الحالي لا يحذفه من الـ commits القديمة.

> **درس مهم جداً للطالب:** بمجرد أن يُرفَع أي سرّ (كلمة سر، مفتاح API) إلى مستودع Git — خصوصاً إن كان قد دُفع (push) إلى GitHub — **يجب اعتباره مكشوفاً للجميع بشكل نهائي**، حتى لو حُذف لاحقاً من الكود. الحل الوحيد الصحيح هو **إبطال (revoke/rotate) المفتاح القديم من مصدره** (هنا: `console.groq.com`) وإصدار مفتاح جديد. حذف السطر من الكود لا يكفي أبداً.

---

## 8. إصلاح أخطاء حقيقية وجدت أثناء العمل (Debugging)

### مشكلة الإشعارات: `_id` مقابل `id`

عند اختبار ميزة "تعليم الإشعار كمقروء عند الضغط عليه"، ظهرت مشكلة: كانت الدالة `GET /users/me/notifications` تُعيد **مستندات Mongoose الخام (raw documents)** مباشرة، والتي يكون حقل تعريفها `_id` (بشرطة سفلية، وهو الاسم الداخلي الذي تستخدمه MongoDB)، وليس `id` (كما يتوقعه التطبيق).

النتيجة: كان كل إشعار يصل للتطبيق بحقل `id` قيمته `undefined`، فكانت أي محاولة "لتعليمه كمقروء" تفشل لأنه لا يوجد `id` صحيح لتحديد أي إشعار بالضبط.

الحل: تمت معالجة الرد بنفس الطريقة المستخدمة في كل الـ endpoints الأخرى (تحويل `_id` إلى `id` قبل إرسال الرد للتطبيق).

> **درس عملي:** هذا مثال حقيقي على أهمية **الاتساق (Consistency)** بين الخادم والتطبيق في تسمية الحقول. أي اختلاف صغير في اسم حقل (`_id` مقابل `id`) قد يبدو تافهاً، لكنه يُسبب فشلاً كاملاً وصامتاً لميزة كاملة. من هنا أهمية دوال التوحيد مثل `formatUser`/`formatService` المذكورة في القسم 4 — توحيد نقطة واحدة يمنع تكرار هذا النوع من الأخطاء في كل endpoint جديد.

### توجيه تسجيل الخروج (Logout Routing)

كان تسجيل خروج الزبون يُعيده لشاشة `/loginScreen`، بينما تسجيل خروج الأدمن يُعيده لشاشة `/rolleScreen` (شاشة اختيار نوع الحساب) — سلوك غير متسق. تم توحيدهما، فأصبح تسجيل الخروج (لأي نوع حساب) يعيد المستخدم دائماً لشاشة اختيار نوع الحساب.

---

## 9. أمور تُركت عمداً غير مكتملة (Scope Management)

من المهم تعليمياً أن يتعلم الطالب أن **ليس كل شيء يجب إنجازه دفعة واحدة** — إدارة نطاق العمل (Scope) بشكل واضح ومُعلَن هي مهارة أساسية في تطوير البرمجيات الحقيقي. هذه الأمور معروفة ومُوثَّقة، وليست "أخطاء منسية":

- **لا يوجد "تذكرني" (Stay Logged In)** بعد إغلاق التطبيق (مذكور في القسم 5).
- **شريط العروض في الشاشة الرئيسية** لا يزال محتوى ثابتاً — فقط شاشة "Offers" المخصصة مربوطة بالبيانات الحقيقية. لا يوجد نموذج بيانات في الخادم يحدد "أي عرض يظهر في أي تبويب من الشاشة الرئيسية".
- **شاشة البحث** لا تزال تبحث في الفئات فقط من جهة التطبيق. الخادم يحتوي فعلاً على `GET /services/search` (يبحث أيضاً بأسماء الخدمات)، لكن ربطه بواجهة البحث يحتاج قسماً جديداً في نتائج البحث، ولم يكن ذلك ضمن نطاق هذا العمل.
- **شاشة المساعدة والدعم** (الأسئلة الشائعة/ساعات العمل) لا تزال نصاً ثابتاً، مع أن endpoints العمل موجودة وتعمل فعلياً.
- **شاشة "إضافة خدمة" في لوحة تحكم الأدمن** لا تحتوي على خيار لاختيار أخصائي معيّن (لم يكن موجوداً من الأساس). الخادم الآن يختار تلقائياً أخصائياً مستخدَماً سابقاً في نفس الفئة عند غياب الاختيار، لمنع فشل إنشاء الخدمة، لكن الحل الأمثل يحتاج واجهة اختيار حقيقية.
- الضغط على بطاقة **"الخدمات الشائعة"** في الشاشة الرئيسية يمرّر بيانات مبسّطة فقط (الاسم/السعر/الصورة)، فتظهر أقسام "About" و"Benefits" فارغة في شاشة التفاصيل — وهذه مشكلة موجودة من قبل هذا العمل أصلاً، وحلّها يحتاج جلب تفاصيل الخدمة كاملة عند الضغط.

---

## 10. خطوات تشغيل المشروع محلياً

هذا القسم يشرح كل ما تحتاجه لتشغيل المشروع كاملاً (خادم + تطبيق) على جهاز لم يُشغَّل عليه المشروع من قبل.

### المتطلبات الأساسية (تُثبَّت مرة واحدة فقط على الجهاز)

| البرنامج | الاستخدام | ملاحظة |
|---|---|---|
| **Git** | لنسخ المشروع من GitHub | |
| **Node.js** (نسخة 18 فأعلى) | لتشغيل الخادم الخلفي (Backend) | يأتي مع `npm` تلقائياً |
| **MongoDB Community Server** | قاعدة البيانات | يمكن تشغيله كخدمة ويندوز تعمل تلقائياً بعد التثبيت |
| **Flutter SDK** | لتشغيل التطبيق (الواجهة الأمامية) | تأكد بعد التثبيت بأمر `flutter doctor` أنه لا توجد مشاكل |

### الخطوة 0: نسخ المشروع

```bash
git clone <رابط المستودع على GitHub>
cd Beauty_Salon
```

### الخطوة 1: قاعدة البيانات (MongoDB)

- بعد تثبيت MongoDB، تأكد أنه يعمل كخدمة:
  ```powershell
  Get-Service MongoDB
  ```
- إن كانت الحالة `Stopped`، شغّلها بـ: `Start-Service MongoDB`
- لا حاجة لإنشاء قاعدة البيانات يدوياً — Mongoose (المكتبة التي يستخدمها الخادم) تُنشئها تلقائياً عند أول اتصال، بالاسم المكتوب في `MONGODB_URL`.

### الخطوة 2: ملف البيئة `backend/.env` (**الخطوة الأهم** — لا يُرفع هذا الملف إلى Git لأنه يحوي أسراراً)

انسخ الملف النموذجي وأنشئ نسختك الخاصة:

```bash
cd backend
cp .env.example .env
```

ثم افتح `backend/.env` واملأ القيم التالية:

| المتغيّر | الشرح | إلزامي؟ |
|---|---|---|
| `PORT` | المنفذ الذي يعمل عليه الخادم | اختياري (الافتراضي `3000` إن تُرك فارغاً؛ استخدم `4000` إن كان المنفذ `3000` مشغولاً بمشروع آخر على جهازك) |
| `MONGODB_URL` | رابط الاتصال بقاعدة البيانات، مثلاً `mongodb://127.0.0.1:27017/belle_beauty_salon` | إلزامي |
| `JWT_SECRET_KEY` / `REFRESH_JWT_SECRET_KEY` | مفتاحان سريان لتوقيع بطاقات الدخول (JWT) — يكفي أي نص عشوائي طويل | إلزامي |
| `ADMIN_NAME` / `ADMIN_PHONE` / `ADMIN_EMAIL` / `ADMIN_PASSWORD` | بيانات حساب المدير الذي ينشئه سكربت `seed` | إلزامي لتشغيل `npm run seed` |
| `DEMO_NAME` / `DEMO_EMAIL` / `DEMO_PHONE` / `DEMO_PASSWORD` | بيانات الحساب التجريبي للزبون (نفس بيانات جدول الحسابات التجريبية أدناه) | إلزامي لتشغيل `npm run seed` |
| `EMAIL_HOST` / `EMAIL_PORT` / `EMAIL_SECURE` / `EMAIL_USER` / `EMAIL_PASSWORD` / `EMAIL_FROM` | إعدادات إرسال بريد إلكتروني حقيقي (تستخدمها ميزتا "تأكيد الحساب بالبريد" و"استرجاع كلمة السر") | اختياري — إن تُركت فارغة، هاتان الميزتان فقط لن تعملا، وباقي التطبيق يعمل طبيعياً |
| `GEMINI_API_KEY` | مفتاح Gemini API | **غير مستخدم حالياً** — الشات بوت الحالي (`chat.controller.js`) بوت بسيط يعمل بمطابقة كلمات مفتاحية محلياً، بدون أي اتصال بخدمة ذكاء اصطناعي خارجية، فلا حاجة لتعبئة هذا المفتاح |
| `NODE_ENV` | بيئة التشغيل | ضع `development` |

> ملاحظة عن البريد الإلكتروني: إن استخدمت Gmail، يجب توليد "App Password" مخصص من إعدادات حساب Google (وليس كلمة سر حسابك العادية) لأن Gmail يمنع تسجيل الدخول بكلمة السر العادية من تطبيقات خارجية.

### الخطوة 3: تشغيل الخادم الخلفي (Backend)

```bash
cd backend
npm install          # فقط أول مرة — لتنصيب كل الحزم (dependencies) المطلوبة
npm run seed         # لتعبئة بيانات تجريبية (يمكن إعادة تشغيله في أي وقت لإعادة الضبط)
npm start            # يبدأ الخادم — راقب الطرفية لمعرفة رقم المنفذ الفعلي (PORT)
```

عند النجاح تظهر هذه الأسطر في الطرفية:
```
Connected to database successfully
Server is running successfully
```

للتأكد من عمل الخادم من متصفح أو بأمر curl:
```bash
curl http://localhost:4000/api/health
```
يجب أن يرجع: `"API is Healthy"`

### الخطوة 4: تشغيل الواجهة الأمامية (Frontend)

```bash
cd frontend
flutter pub get         # لتنصيب حزم Flutter/Dart المطلوبة
flutter run -d windows  # لتشغيل التطبيق على ويندوز
```

أجهزة أخرى ممكنة بدل `windows`: `chrome` (متصفح)، أو اسم محاكي/جهاز أندرويد (يظهر عبر `flutter devices`).

**مهم عند التشغيل على غير Windows/جهاز الكمبيوتر نفسه:** التطبيق يتواصل مع الخادم عبر عنوان ثابت في `frontend/lib/services/api_service.dart` (المتغيّر `apiBaseUrl`)، وهو مضبوط افتراضياً على `http://localhost:4000/api/v1`. غيّره حسب حالتك:
- محاكي أندرويد (Android Emulator): `http://10.0.2.2:4000/api/v1`
- جهاز أندرويد/آيفون حقيقي على نفس شبكة Wi-Fi: `http://<عنوان IP الخاص بجهاز الكمبيوتر>:4000/api/v1`
- إن غيّرت `PORT` في `.env`، غيّر رقم المنفذ هنا أيضاً ليطابقه.

### حسابات تجريبية جاهزة (موجودة أيضاً كأزرار "Use" في شاشة تسجيل الدخول)

| النوع | البريد الإلكتروني | كلمة السر |
|---|---|---|
| زبون (Customer) | `kelly@belle.com` | `Belle1234` |
| أدمن (Admin) | `admin@belle.com` | `Admin1234` |

### مشاكل شائعة وحلولها (Troubleshooting)

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| رسالة "خطأ في الاتصال" / "تعذّر الوصول إلى الخادم" داخل التطبيق | الخادم الخلفي غير مُشغَّل، أو `apiBaseUrl` لا يطابق منفذ/عنوان الخادم الفعلي | شغّل الخادم أولاً وتأكد بـ `curl http://localhost:4000/api/health`، وراجع `apiBaseUrl` |
| `Mongodb Error: ...` عند تشغيل `npm start` | خدمة MongoDB متوقفة، أو `MONGODB_URL` خاطئ | `Start-Service MongoDB` وتأكد من قيمة `MONGODB_URL` في `.env` |
| `Email server connection failed` تظهر في طرفية الخادم عند بدء التشغيل | متغيرات `EMAIL_*` فارغة أو خاطئة | طبيعي إن لم تُضبط بعد — لن تعمل فقط ميزتا تأكيد البريد واسترجاع كلمة السر، وباقي التطبيق يعمل بشكل طبيعي |
| `EADDRINUSE` أو "port already in use" | منفذ الخادم مستخدم من برنامج آخر | غيّر `PORT` في `.env` إلى رقم آخر (مثلاً `4001`)، أو أوقف العملية التي تستخدم المنفذ |
| فشل بناء تطبيق Flutter بسبب امتلاء القرص (`There is not enough space on the disk`) | قرص `C:` شبه ممتلئ ومجلد الملفات المؤقتة لـ Flutter موجود عليه | وجّه المتغيرات `TEMP`/`TMP` مؤقتاً إلى مجلد على قرص آخر فيه مساحة قبل تشغيل `flutter run`، أو حرّر مساحة على القرص `C:` |
| الشات بوت يرد بردود بسيطة جداً وليست ذكية | هذا هو التصميم الحالي — بوت كلمات مفتاحية محلي بدون خدمة ذكاء اصطناعي خارجية | هذا طبيعي وليس خطأ؛ لا حاجة لأي مفتاح API لتشغيله |

---

## 11. خلاصة: أهم المفاهيم التي تعلمناها من هذا المشروع

هذا المشروع، على صغره الظاهري، يُلخّص عدداً كبيراً من المفاهيم الأساسية في هندسة البرمجيات الحديثة:

1. **بنية Client-Server** وكيف يتواصل التطبيق مع الخادم عبر REST API بصيغة JSON.
2. **الفرق بين قواعد بيانات SQL و NoSQL**، ولماذا اختيار MongoDB (Document Database) منطقي لهذا النوع من التطبيقات.
3. **نمط ODM (Mongoose)** لتعريف شكل البيانات بلغة برمجية مباشرة (Schemas)، بدل SQL خام.
4. **فصل الاهتمامات (Separation of Concerns)** عبر تقسيم الخادم إلى Models/Controllers/Routes/Utils.
5. **حماية المسارات بواسطة Middleware** (auth + role) لتطبيق قواعد أمنية مرة واحدة على مجموعة كاملة من الروابط.
6. **JWT للمصادقة**، وأهمية ألا يقرر التطبيق (Client) صلاحيات المستخدم بمفرده.
7. **متغيرات البيئة (.env)** لإبقاء الإعدادات الحساسة خارج الكود المصدري.
8. **درس أمني جوهري:** الأسرار (API Keys) لا يجوز أبداً أن تصل لجهاز المستخدم، وأي سرّ يُرفع لـ Git يجب اعتباره مكشوفاً نهائياً.
9. **إدارة الحالة التفاعلية (Reactive State Management)** في Flutter عبر GetX و `Obx`.
10. **طبقة خدمة موحدة (Service Layer)** للتعامل مع الشبكة من نقطة واحدة في التطبيق.
11. **Seed Scripts** لإدارة بيانات تجريبية قابلة لإعادة الضبط بسهولة.
12. **قراءة الأخطاء الحقيقية** (مراجع نماذج غير موجودة، أسماء حقول خاطئة، عدم اتساق `_id`/`id`) كمهارة تصحيح (Debugging) لا غنى عنها.
13. **إدارة نطاق العمل (Scope Management)**: توثيق ما لم يُنجَز عمداً بوضوح، بدل ترك المستخدم يظن أن شيئاً "معطّل" دون علمه.

> **للطالب:** أفضل طريقة للاستفادة من هذا الملف هي عدم الاقتصار على القراءة — افتح الملفات المذكورة فعلياً (`backend/src/models/Category.js`، `backend/src/controllers/auth.controller.js`، `frontend/lib/services/api_service.dart`، إلخ) وقارن بين الشرح هنا والكود الحقيقي. الفهم الحقيقي يأتي من الربط بين الشرح النظري والكود الفعلي أمامك.
